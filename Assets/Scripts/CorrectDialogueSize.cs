using UnityEngine;
using System.Collections;

public class CorrectDialogueSize : MonoBehaviour {

	private UIRoot uiroot;

	// Use this for initialization
	void Start () {
		GetComponent<UISprite> ().enabled = false;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
